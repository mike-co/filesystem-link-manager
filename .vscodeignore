# VS Code extension ignore file for `vsce package`
# Keep only runtime artifacts (bundled output) and required metadata.

# -----------------------------
# Editor, tooling and caches
# -----------------------------
.vscode/
.vscode-test/
node_modules/

# -----------------------------
# VCS and repository metadata
# -----------------------------
.git/
.github/
.copilot-tracking/

# -----------------------------
# Tests, coverage and CI artifacts
# -----------------------------
coverage/
**/__tests__/
**/*.test.*
**/*.spec.*
test/

# -----------------------------
# Source files (we ship bundled JS in dist/)
# -----------------------------
src/
out/
**/*.ts
**/*.tsx
tsconfig.json
tsconfig.tests.json
jest.config.js
webpack.config.js

# -----------------------------
# Build artifacts and maps
# -----------------------------
*.log
*.tsbuildinfo

# -----------------------------
# Samples and other repo-only artifacts
# -----------------------------
samples/

# -----------------------------
# Editor/OS files
# -----------------------------
.DS_Store
Thumbs.db

# -----------------------------
# Misc repo helper files not needed in VSIX
# -----------------------------
.yarnrc
vsc-extension-quickstart.md
.gitignore
.prettierignore
.vscode-test.mjs
eslint.config.mts

# -----------------------------
# Keep these files in the package:
# - dist/ (bundled extension - includes all dependencies)
# - assets/ (icons and other assets)
# - package.json
# - README.md / readme.md
# - LICENSE, LICENSE.txt
# - CHANGELOG.md / changelog.md
# Do NOT add exclusion rules for those files above.
# -----------------------------
